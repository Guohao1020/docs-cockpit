你在 docs-cockpit sprint **{{ module.sprint or "(unscheduled)" }}** 修一个 bug 子任务 `{{ subtask.id }}`(module {{ module.id }} · {{ module.title }})。

## Bug

**{{ subtask.title }}**

{{ module.desc or "" }}

## 参考(用 Read 工具按需拉)

{% if subtask.code_anchors %}
**疑似问题位置**:
{%- for ca in subtask.code_anchors %}
- `{{ ca.path_only or ca.path }}{% if ca.lines %}:{{ ca.lines }}{% endif %}`{% if not ca.exists %} ⚠ {{ ca.warning }}{% endif %}
{%- endfor %}
{% endif %}
{% if subtask.doc_anchors %}
**关联 issue / spec**:
{%- for da in subtask.doc_anchors %}
- `{{ da.raw }}`{% if not da.exists %} ⚠ {{ da.warning }}{% endif %}
{%- endfor %}
{% endif %}
{% if linked_docs %}
**关联文档**:
{%- for doc in linked_docs %}
- `{{ doc.path }}`{% if doc.title %} · {{ doc.title }}{% endif %}
{%- endfor %}
{% endif %}

## 范式

按 spec/plan 工作流实施(Read 参考 → 改代码 → 验证 → diff 报告)。`fix` 子任务的特定要求 · 守 Iron Law:**找 root cause · 不要只修 demo path · 加 regression test 防回归**。给 minimal repro + 解释根因 + 单测在哪。

{% include "_caller_aware_sync.md.j2" %}
