你在 docs-cockpit 给 module **{{ module.id }} · {{ module.title }}** 做 anchor 二次确认。

## 任务定义

`lint_subtask_anchors` 只能抓「subtask 0 anchor」死规则 · **不能判 anchor 准不准**。
你的工作是把每个 subtask claimed 的 `code` / `docs` anchor 真去 Read 一遍 · 跟 subtask
title + module desc 对照 · 给 4 档 verdict:

| Verdict | 含义 | 处理 |
|---|---|---|
| ✅ `accurate`  | anchor 文件存在 · 行范围 / 章节真指到跟 title 相关的内容 | 不动 |
| ⚠️ `partial`  | 文件在 · 但 line range / 章节略偏 · 命中了相关代码的邻居 | 建议调整范围 |
| ❌ `wrong`    | 文件不存在 / 文件在但内容跟 title 完全无关 | 必须改 anchor 或 mark TODO |
| ❓ `missing`  | subtask 完全没 anchor | 建议补 `code:` / `docs:` |

**driver-seat 信任来自精度** · 错 anchor 比缺 anchor 伤害更大 · 找不到对应 section 就给
`❌ wrong` + `# TODO: …` 注释 · 不要瞎猜。

## 执行模式 · 二选一(先判断你是谁)

- **A · 有文件编辑工具**(Claude Code / Cursor / Codex CLI · 即能 Read + Edit 本地文件):
  1. 对每个 subtask · Read 它声明的每个 anchor target file(给 lines 时只读对应行段)
  2. 判 verdict · 列在「Verify 报告」section
  3. ❌ / ⚠️ 的项目 · 直接 Edit `docs/spec/module/{{ module.id }}-*.md` 的 body checklist 行
     · 改 `@code:` / `@docs:` annotation
  4. ❓ 的项目 · 建议补什么 anchor · 同样直接 Edit
  5. 跑 `docs-cockpit build` · 验证 anchor 落到 state.json · 报简短改动 summary

- **B · 没文件编辑工具**(浏览器 LLM):输出下面格式的 YAML patch · 用户走
  `docs-cockpit apply-patch docs/spec/module/{{ module.id }}-*.md patch.yaml --apply` 落地。

## Module 上下文

- **id** · `{{ module.id }}`
- **title** · {{ module.title }}
- **desc** · {{ module.desc or "(no desc)" }}
- **status** · {{ module.status }} · {{ module.progress or 0 }}%
{% if module.sprint %}- **sprint** · {{ module.sprint }}{% endif %}

## 待验证 subtask 清单

{% for st in subtasks %}
### `{{ st.id }}` · {{ st.title }}

- **status** · {{ st.status }}
{% if st.code_anchors -%}
- **Code anchors**({{ st.code_anchors | length }}):
{% for ca in st.code_anchors %}
  - `{{ ca.path }}`{% if ca.lines %} · lines `{{ ca.lines }}`{% endif %}{% if ca.path_only and ca.path_only != ca.path %} · file=`{{ ca.path_only }}`{% endif %}
{%- endfor %}
{%- elif st.code -%}
- **Code anchors**(legacy str list):
{% for c in st.code %}
  - `{{ c }}`
{%- endfor %}
{%- else -%}
- **Code anchors** · ⚠️ (none)
{%- endif %}
{% if st.doc_anchors -%}
- **Doc anchors**({{ st.doc_anchors | length }}):
{% for da in st.doc_anchors %}
  - `{{ da.raw or da.path }}`{% if da.section %} · §`{{ da.section }}`{% endif %}{% if da.lines %} · lines `{{ da.lines }}`{% endif %}
{%- endfor %}
{%- elif st.docs -%}
- **Doc anchors**(legacy str list):
{% for d in st.docs %}
  - `{{ d }}`
{%- endfor %}
{%- else -%}
- **Doc anchors** · ⚠️ (none)
{%- endif %}

{% endfor %}

## Linked docs(已 embed 摘要 · 完整 doc 在 repo · 按需 Read)

{% for doc in linked_docs %}
### {{ doc.title or doc.path }}

`{{ doc.path }}`

{{ doc.summary or "(no content embedded)" }}

---
{% endfor %}

{% if repo_root %}
## Repo 根
`{{ repo_root }}`
{% endif %}

## 输出 · 模式 B 走这个 YAML patch 格式

```yaml
# verify patch for {{ module.id }}
# generated by docs-cockpit verify · v0.17.0+
subtasks:
{% for st in subtasks %}
  - id: {{ st.id }}
    # 每个 anchor 给一行 verify_note · 形如:
    #   - <anchor> · ✅|⚠️|❌|❓ · <一句话说为什么>
    verify_notes:
      - "examples/anchor.py:1-20 · ✅ accurate · lines 1-20 是 init 函数 · 跟 title 对得上"
    # ❌ / ⚠️ / ❓ 的项目在下面给修正 anchor · ✅ 的不用列(原 anchor 保留)
    {% if st.code_anchors or st.code -%}
    # code: ["docs_cockpit/foo.py:42-89"]   # 调整 / 替换 · 不写 = 保留原
    {%- endif %}
    {% if st.doc_anchors or st.docs -%}
    # docs: ["docs/plans/p.md#§6.2"]        # 调整 / 替换 · 不写 = 保留原
    {%- endif %}

{% endfor %}
```

## 自检 · 报告前确认

- [ ] 每个 subtask 都给了至少 1 行 `verify_note`
- [ ] ❌ wrong 的 subtask 给了替换 anchor 或明确的 `# TODO`
- [ ] ❓ missing 的 subtask 给了建议 anchor(基于 module desc + linked docs 推断)
- [ ] 没瞎猜:找不到对应 section 用 ❌ + `# TODO` · 不编造行号
- [ ] 模式 A:跑了 `docs-cockpit build` · 验证 anchor 落 state.json
