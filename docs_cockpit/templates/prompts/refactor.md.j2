你在 docs-cockpit sprint **{{ module.sprint or "(unscheduled)" }}** refactor 一段代码 · 子任务 `{{ subtask.id }}`(module {{ module.id }} · {{ module.title }})。

## 目标

**{{ subtask.title }}**

{{ module.desc or "" }}

## 参考(用 Read 工具按需拉)

{% if subtask.code_anchors %}
**Refactor 涉及代码**:
{%- for ca in subtask.code_anchors %}
- `{{ ca.path_only or ca.path }}{% if ca.lines %}:{{ ca.lines }}{% endif %}`{% if not ca.exists %} ⚠ {{ ca.warning }}{% endif %}
{%- endfor %}
{% endif %}
{% if subtask.doc_anchors %}
**设计依据**:
{%- for da in subtask.doc_anchors %}
- `{{ da.raw }}`{% if not da.exists %} ⚠ {{ da.warning }}{% endif %}
{%- endfor %}
{% endif %}
{% if linked_docs %}
**关联设计**:
{%- for doc in linked_docs %}
- `{{ doc.path }}`{% if doc.title %} · {{ doc.title }}{% endif %}
{%- endfor %}
{% endif %}

## 范式

按 `docs-cockpit-author` §11 实施。`refactor` 子任务的特定要求 · **Behavior preserving** · 现有测试必须全过 · Beck 「make the change easy, then make the easy change」· 不要 refactor + behavior change 一起做。

{% include "_caller_aware_sync.md.j2" %}
