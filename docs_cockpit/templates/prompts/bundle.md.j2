你在 docs-cockpit 串行执行 **{{ subtasks | length }} 个 subtask**(bundle)· 跨 {{ modules | length }} 个 module。

## Bundle 概览

**Cohesion verdict**:{{ score.verdict }} · avg cohesion {{ score.avg_cohesion }} · max conflict {{ score.max_conflict }}
{% for note in score.notes %}- {{ note }}
{% endfor %}

**推荐执行顺序**(沿 depends_on chain + 同 file 优先):
{% for s in subtasks %}{{ loop.index }}. `{{ s.id }}` · {{ s.title }} · ({{ s._module_id }} · sprint {{ s._module_sprint or "(none)" }})
{% endfor %}

{% if missing %}
**⚠ 找不到的 subtask id**(已跳过):{{ missing | join(", ") }}
{% endif %}

## 共享 module 上下文({{ modules | length }} module)

{% for m in modules %}
### `{{ m.id }}` · {{ m.title }}
- **Sprint**:{{ m.sprint or "(unscheduled)" }} · Status: {{ m.status }} · Progress: {{ m.progress }}%
- **Desc**:{{ m.desc or "(empty)" }}
{% if m.depends_on %}- **Depends on**:{{ m.depends_on | join(", ") }}{% endif %}

{% endfor %}

## 共享参考(用 Read 工具按需拉 · 跨 subtask 复用 · 别让我把全文复制给你)

{% if shared_code_files %}
**代码 file**(N subtask 共用 · 一次读省 context):
{%- for path, sids in shared_code_files.items() %}
- `{{ path }}` · 被 {{ sids | length }} subtask 引用:`{{ sids | join("`, `") }}`
{%- endfor %}
{% endif %}

{% if shared_doc_anchors %}
**Doc anchor**:
{%- for raw, sids in shared_doc_anchors.items() %}
- `{{ raw }}` · 被 {{ sids | length }} subtask 引用
{%- endfor %}
{% endif %}

## 子任务详情(按推荐顺序)

{% for s in subtasks %}
### {{ loop.index }}. `{{ s.id }}` · {{ s.title }}

- **Module**:{{ s._module_id }}
- **Status**:{{ s.status }}

{% if s.code_anchors %}
**代码 anchor**:
{%- for ca in s.code_anchors %}
- `{{ ca.path }}`{% if not ca.exists %} ⚠ {{ ca.warning }}{% endif %}
{%- endfor %}
{% endif %}
{% if s.doc_anchors %}
**Doc anchor**:
{%- for da in s.doc_anchors %}
- `{{ da.raw or da.path }}`{% if not da.exists %} ⚠ {{ da.warning }}{% endif %}
{%- endfor %}
{% endif %}

{% endfor %}

## 完成 + 同步驾驶舱(N 个 subtask 都要勾完才报告)

按 `docs-cockpit-author` §11 + §14(bundle heuristics)spec/plan 范式实施。有 fs 工具(Claude Code / Cursor / Codex CLI)就一次干完:

1. 按推荐顺序串行实施(沿 depends_on · 同 file 一起改省 review)
2. 每完成一个 subtask · 编辑对应 module MD body checklist · 把那行 `[ ]` 改 `[x]` · 行尾追 `@code:path:lines` / `@docs:path#§N.M`(如果引用了新文件 / 新章节)
3. 全部 {{ subtasks | length }} 个 subtask 完成 · 跑一次 `docs-cockpit build` · 验证 state.json 里所有 done=true + anchor 落到位
4. 简短报告:每个 subtask 做了什么(可以合并 sentence)+ 改了哪些 file + build 是否干净。用户硬刷新(Ctrl+Shift+R)看驾驶舱 N 个 checkbox 自动勾上

没 fs 工具就输出 N 行 YAML patch:

```yaml
# bundle patch
subtasks:
{% for s in subtasks %}  - id: {{ s.id }}
    status: done
    # 如果你写了新代码 · 列 code anchor
    # code: ["<file>:<start>-<end>"]
{% endfor %}```

按 `docs-cockpit-author` §14(Bundle heuristics)· **默认走 fs-edit 路径** · 副驾的价值就是不让用户重复打字。
