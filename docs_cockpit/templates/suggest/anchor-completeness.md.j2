你正在帮我补 docs-cockpit module **`{{ module.id }} · {{ module.title }}`** 缺失的 code / docs anchor。

## 诊断 · 缺 anchor 的 subtask

{% set missing = [] %}
{% for sub in module.subtasks %}
{%- if not sub.code and not sub.code_anchors -%}
{%- set _ = missing.append(sub) -%}
{%- elif not sub.docs and not sub.doc_anchors -%}
{%- set _ = missing.append(sub) -%}
{%- endif -%}
{% endfor %}

{% for sub in missing %}
- `{{ sub.id }}` · {{ sub.title }}
  {%- if not sub.code and not sub.code_anchors %} · ⚠ 缺 `@code:` anchor{% endif %}
  {%- if not sub.docs and not sub.doc_anchors %} · ⚠ 缺 `@docs:` anchor{% endif %}
{% endfor %}

## 为什么 anchor 很重要

`docs-cockpit-author` §11.3 + §3.1.2 + §3.1.3:
- **code anchor** · subtask 对应代码 `path:start-end` · 让 Refine prompt / Copy prompt 能 inject 上下文 · 让 AI 知道在哪儿改
- **docs anchor** · subtask 对应 plan / RFC `path#§N.M` 或 `:start-end` · 让用户在 dashboard 点 doc 图标看到精确切片
- 没 anchor 的 subtask · prompt 上下文丢失 · AI 只能拿 desc 一行字猜

## Anchor 格式

| 形式 | 例 |
|---|---|
| 单 file:lines | `worker/x.py:42-89` |
| 单 file 整文件 | `worker/x.py` |
| 多 anchor list | `[a.py:1-10, b.py:5-9]` |
| heading slug | `plan.md#§6.2` |

## 工作流

按 `docs-cockpit-author` §11.1 「The standard authoring flow」5 步:

1. Read 关联文档(下方 list)· 理解 module 在 plan 里属于哪个 § / RFC
2. 跨参考拆 subtask → plan section
3. Read repo · 找代码落地点 / 行号
4. 给每条 missing 的 subtask 补 `@code:` / `@docs:` annotation(body checklist)或 `code:` / `docs:` 字段(frontmatter)
5. self-check · 同 plan section 不要被多 subtask 重复 anchor

## Module 上下文(用 Read 工具按需拉)

{% if linked_docs %}**关联文档**:
{%- for doc in linked_docs %}
- `{{ doc.path }}`{% if doc.title %} · {{ doc.title }}{% endif %}
{%- endfor %}
{% endif %}

## 输出

有 fs 工具(Claude Code / Cursor / Codex CLI):直接 edit `{{ 'docs/spec/module/' + module.id + '.md' }}` body checklist 行尾追 `@code:` / `@docs:` annotation · 跑 `docs-cockpit build` 验证 anchor 落到 state.json · 给我简短报告。

没 fs 工具:输出 patch YAML:

```yaml
# patch for {{ module.id }}
subtasks:
{% for sub in missing %}  - id: {{ sub.id }}
    code: ["<file>:<start>-<end>"]
    docs: ["<plan-or-rfc>:<lines or #§slug>"]
{% endfor %}```
