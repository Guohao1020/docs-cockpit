你正在帮我改写 docs-cockpit module **`{{ module.id }} · {{ module.title }}`** 的 desc 字段。

## 当前 desc

```
{{ module.desc or "(empty)" }}
```

## 问题诊断

{% if not module.desc %}- desc 为空 · dashboard 在 module card 上没东西显示 · prompt 上下文丢失
{% elif module.desc | length < thresholds.desc_min_chars %}- desc 过短({{ module.desc | length }} chars < {{ thresholds.desc_min_chars }} chars 阈值)· 信息密度不足
{% else %}- desc 含有过泛词汇(TBD / todo / 待补 等)· 应被具体内容替换
{% endif %}

## 重写要求

`docs-cockpit-author` §2 + §11 范式 · 一行 desc 应该说清:
- module 是什么(技术名词 / 文件 / CLI 子命令)
- 为什么存在(解决哪个 problem · plan 的哪个 § / RFC)
- 跟 sibling module 的边界(可选 · 1 短句)

好 desc 示例(参考):
> "Single-source-of-truth for frontmatter schema · 8 sections + validator Issue.reference 反向指向 · 跟 build.py / lint 双向引用"

坏 desc 示例:
> "Author skill 的实现" · "TBD" · "重要 module"

## Module 上下文(用 Read 工具按需拉)

{% if linked_docs %}**关联文档**:
{%- for doc in linked_docs %}
- `{{ doc.path }}`{% if doc.title %} · {{ doc.title }}{% endif %}
{%- endfor %}
{% endif %}

## 输出

改写一个 1 行 desc(<= 100 chars)· 直接编辑 `{{ 'docs/spec/module/' + module.id + '.md' }}` frontmatter 的 `desc:` 字段。改完跑 `docs-cockpit build` 验证 dashboard 显示。
