你正在帮我 recompose docs-cockpit module **`{{ module.id }} · {{ module.title }}`** 的 subtask 拆解粒度。

## 诊断

{% set n = (module.subtasks | length) %}
{% if n > thresholds.subtasks_max %}
- 当前 {{ n }} 个 subtask · 超 max {{ thresholds.subtasks_max }} 阈值 · **过细**
- 表象:每条 subtask 都 1 行 · 但完成顺序不清楚 · dashboard 长得吓人
- 修法:合并相近 subtask · 或拆 module 成 sibling(M0X-a / M0X-b)
{% elif n < thresholds.subtasks_min %}
- 当前 {{ n }} 个 subtask · 不足 min {{ thresholds.subtasks_min }} 阈值 · **过粗**
- 表象:勾选感太弱 · 实施时容易跑偏(单 subtask 写 3 天)
- 修法:沿 plan / RFC section 拆开 · 每条对应一个具体落地点
{% endif %}

## 当前 subtask 清单

{% for sub in module.subtasks %}
- `[{% if sub.done %}x{% else %} {% endif %}]` `{{ sub.id }}` · {{ sub.title }}
{% endfor %}

## 重组原则(`docs-cockpit-author` §11.1 + §3.1.1)

1. **每条 subtask 对应 plan 某个具体 section / RFC 决策** · 不是"实施某个功能"这种过泛
2. **subtask.title 用 plan 里的术语** · 保语义一致 · 让 prompt 渲染时无歧义
3. **粒度 sweet spot · 3-15 条** · 太多说明 module 该拆 · 太少说明 subtask 该再拆
4. **title 改了 id 也改**(sha1 算)· 大改时显式钉 `id:` · 见 §3.1.1 trade-off

## Module 上下文(用 Read 工具按需拉)

{% if linked_docs %}**关联设计 / spec**:
{%- for doc in linked_docs %}
- `{{ doc.path }}`{% if doc.title %} · {{ doc.title }}{% endif %}
{%- endfor %}
{% endif %}

## 输出

输出 recompose 后的新 subtask list(YAML format · 兼容 frontmatter Form A 或 body checklist Form C 任一):

```yaml
# new subtasks for {{ module.id }}
subtasks:
  - id: ...           # 显式 pin 老 id 保 localStorage / cross-ref 不破
    title: ...
    code: ...
  ...
```

或直接 edit `{{ 'docs/spec/module/' + module.id + '.md' }}` body checklist · 跟着跑 `docs-cockpit build`。
