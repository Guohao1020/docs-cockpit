你正在帮我检查 docs-cockpit module **`{{ module.id }} · {{ module.title }}`** 内 subtask 哪些**适合 bundle 一起跑**。

## 范式

参考 `docs-cockpit-author` §14「Bundle heuristics」· 沿 4 维 cohesion + 4 维 conflict 做 cross-check。

## 当前 subtask 清单

{% for sub in module.subtasks %}
- `[{% if sub.done %}x{% else %} {% endif %}]` `{{ sub.id }}` · {{ sub.title }}
  {%- if sub.code_anchors %} · code: {% for ca in sub.code_anchors %}`{{ ca.path_only or ca.path }}{% if ca.lines %}:{{ ca.lines }}{% endif %}`{% if not loop.last %}, {% endif %}{% endfor %}{% endif %}
  {%- if sub.doc_anchors %} · docs: {% for da in sub.doc_anchors %}`{{ da.raw_with_anchor or da.raw or da.path }}`{% if not loop.last %}, {% endif %}{% endfor %}{% endif %}
{% endfor %}

## 你的任务

跑 author skill §14 4 cohesion + 4 conflict 维度 · 对**每对** not-done subtask 算 cohesion 分。输出 markdown table:

```markdown
| Subtask A | Subtask B | Cohesion | Conflict | 建议 |
|---|---|---|---|---|
| M-X1 | M-X2 | high(同 file + 同 plan §) | none | ✅ Bundle |
| M-X1 | M-X3 | low(完全独立) | none | ❌ 别 bundle |
| M-X2 | M-X4 | mid | ⚠ 同 file lines 重叠 | ⚠ 注意 conflict · 分开做安全 |
```

然后给 1-3 个**推荐 bundle 集合**(每个集合 2-5 个 subtask)· 例:

> ### Bundle A · 推荐(高 cohesion)
> M-X1 + M-X2 + M-X5 · 都在 `worker/fsm.py` · 串行做省 review
> 推荐顺序:M-X5 → M-X1 → M-X2(沿 depends_on)
> CLI: `docs-cockpit prompt --bundle M-X5,M-X1,M-X2 --copy`

## Module 上下文(用 Read 工具按需拉)

{% if linked_docs %}**关联文档**:
{%- for doc in linked_docs %}
- `{{ doc.path }}`{% if doc.title %} · {{ doc.title }}{% endif %}
{%- endfor %}
{% endif %}

## 输出

- 一个 cohesion table(覆盖所有 not-done pair · 或 top 10 if N>5)
- 1-3 个 bundle 集合推荐(含推荐顺序 + CLI command)
- 1 个「别 bundle」反例(说明哪些 subtask 别一起做)

若 module 只有 1 not-done subtask · 直接说「单 subtask · 不用 bundle · 走 `docs-cockpit prompt {{ module.id }}` 即可」。
