---
id: DOCS-COCKPIT-V0.11-PLAN-2026-05-17
type: plan
title: "docs-cockpit v0.11 · AI 协作驾驶舱 · execution plan"
status: approved
sprint: "0.11"
owner: harvey
desc: "把 cockpit 从『可视化看板』升级为『AI 协作驾驶舱』· W1 subtask 一等公民 + W3 prompt scaffolding · 不做 W2 LLM 文档优化器(留给 v0.12 根据真实反馈决定)"
created: 2026-05-17
prd_ref:
depends_on: []
blocks: []
---

# Design · docs-cockpit v0.11 · AI 协作驾驶舱

Generated by /office-hours on 2026-05-17
Branch: main · Repo: docs-cockpit (MIT, open source)
Status: **APPROVED** · Mode: Builder · Approach: B (W1 + W3)
Supersedes: (first design doc in this repo · dogfood 起步)
Review: round-1 6/10 → 10 issues fixed → round-2 8/10 → 5 nits fixed → approved 2026-05-17

---

## §0 · driver-seat 角色定位(2026-05-18 round-4 重新框架)

**关键 architectural shift**:driver-seat **不是自给自足的精度引擎** · 它是 **AI 工具(Claude Code / Codex / Cursor)的副驾**。

| 维度 | 旧定位(误读) | 新定位(正确) |
|---|---|---|
| 精度来源 | docs-cockpit 用 python regex / 反向 index 算 | **LLM 来做语义精度** · python 只做解析层(anchor 语法 / 行号 / heading slug) |
| 核心问题(round-3 提的 doc anchor / module-aware highlight) | 怎么用 python 判断 subtask 跟 doc 哪段相关 | **怎么教 AI** 在写 module MD 时直接产出精准 anchor · 或 **让 AI 检查现有 module** 输出 refinement patch |
| docs-cockpit 的核心责任 | 算精度 + 渲染 | **上下文供给(context)+ UI 展示 + 工作流编排** |

**implications**:
- v0.11 alpha.6(split-view UI)· python 仅算解析层精度 · 让 AI 输出的 anchor 能渲染
- v0.11 alpha.7 · **AI-augmented precision 两条线**:
  - 模式 3:升级 `docs-cockpit-author` SKILL.md · 教 Claude 写 module MD 时直接产出精准 anchor
  - 模式 2:split-view 加「🤖 Ask AI to refine this module」按钮 · 走 prompts.js 延伸 · AI 检查现有 module + 输出 YAML patch
- v0.12 · 模式 1 · Claude API build-time 自动 augment + MCP server 直连(去掉 copy-paste)

详见 sub-plan:`docs/plans/P-v0.11-ai-augmented-precision-alpha7-2026-05-18.md`

## §1 · Problem Statement

docs-cockpit 现状是一个能渲染 module/concept/sprint/progress 的 **可视化看板**,但用户体验暴露出 5 个核心缺口:

1. **模块-任务-子任务-spec/plan/RFC 关联不到位** · subtasks 目前是 `list[str]` · 没有 ID · 没法跨 build 稳定追踪 · 没法跟具体 spec/plan/RFC 双向锚定
2. **子任务无法直接定位到代码位置** · 用户写"补 BrowserVendor abstraction"但 cockpit 不知道这指的是 `sourcery/worker/browser_vendor.py:42-89`
3. **任务清单状态控制不闭环** · localStorage 已经能勾选,但状态不回流到 MD · 长期会跟 source-of-truth 漂移
4. **用户文档质量参差** · 新用户经常 frontmatter 残缺 / subtasks 是 stub · cockpit 没法引导他们写出合规的 source MD
5. **驾驶舱不能执行任务** · 用户看到一个 in-progress subtask · 还得手动去拼"我现在要做什么 / 哪些文件 / 哪些 spec / 哪些前置" · cockpit 应该一键给出可执行的 prompt

这 5 个缺口本质上是同一件事:**cockpit 还停留在「展示」· 没有进入「驱动」**。

## §2 · What Makes This Cool

把 cockpit 从「项目状态展示器」升级为「AI 协作驾驶舱」。落地后的「驾驶员体验」是:

- 用户打开 cockpit · 看到 6 个 in-progress module · 每个 module drawer 里有真实子任务 checklist
- 点开任意 subtask · 看到 `code:` anchor 直链到 `sourcery/worker/browser_vendor.py:42-89` 的对应行 · 旁边显示关联的 spec §2.1 摘要
- 点「复制提示词」· 拼好的 prompt 进剪贴板 · 包含:module spec § / subtask 详情 / linked docs 摘要 / 代码 anchor 内容 / 当前 status
- 用户粘到 Claude 跑 · Claude 知道现在要做什么 / 在哪里改 / 验收标准是什么
- 跑完 · Claude 输出 frontmatter patch · 用户审阅后落回 MD · 状态闭环

这把 docs-cockpit 从「写好文档之后可视化用」升级到「跟 AI 一起做事的指令中心」。

## §3 · Constraints

- **MIT 开源 · 不能 vendor lock** · prompt template 必须用通用 Jinja2 · 不绑死 Claude / Anthropic SDK
- **下游 Sourcery + bastion 已经在生产用 v0.10** · v0.11 必须向后兼容旧 `subtasks: list[str]` 格式
- **Plugin 不能 pip-install** · CLI 升级走 `docs-cockpit upgrade` · plugin marketplace cache 必须随 minor bump 失效
- **HTML template 是 single-file `__DOCS_JSON__` 替换** · 不能引入运行时 JS framework · prompt 必须在 build 时渲染为静态 string 嵌入
- **不能侵入式改用户 MD** · LLM 改写一律走「生成 prompt 让用户跑 Claude」路径 · MD 永远是 SSOT
- **CHANGELOG / SemVer 偏移**:任何 SKILL.md 变更都是 minor · 这次同时改 `docs-cockpit-author` / `docs-cockpit` 两个 skill + build.py · 必须 0.11.0 minor

## §4 · Premises (locked in Phase 3)

1. **方向 premise** · v0.11+ 不是更精致的看板 · 是从「可视化看板」升级到「AI 协作驾驶舱」
2. **Schema 演进 premise** · `subtasks` 从字符串 list 升级为对象数组 · 字段至少 `id / title / status / code? / docs?` · **minor · 完全向后兼容旧 string 格式**(validator 把 string 当 `title` · 自动 normalize · 不报 error)
3. **Prompt scaffolding 边界 premise** · v1 面向人 copy-paste · 不直连 Claude · MCP / plugin hook 是 v2 范围
4. **Trust premise** · 「自动优化文档」不能侵入式改用户 MD · MVP 走「生成 prompt 让用户跑」· 保留 MD 是 SSOT 的信任锚
5. **Hard gate premise** · 本轮 office-hours 只设计 · 不实施 · 实施由后续 task 触发(/plan-eng-review 或直接动手)

## §5 · Approaches Considered

### Approach A · MVP 数据层 (W1 only)
**Summary**:仅升级 subtask 为一等公民 · 不做 prompt scaffolding · 不做 LLM 优化器
**Effort**:M (2-3 周)
**Risk**:Low

### Approach B · 数据层 + Prompt scaffolding (W1 + W3) ✅ CHOSEN
**Summary**:在 A 之上加 prompt template 引擎 · cockpit drawer 增加「复制提示词」按钮 · LLM 优化器留 v2
**Effort**:M-L (3-4 周)
**Risk**:Low-Med

### Approach C · 全功能 (W1 + W2 + W3)
**Summary**:B 之上加 `docs-cockpit suggest` 子命令(LLM 改写用户 MD 的 prompt 生成)
**Effort**:XL (4-6 周)
**Risk**:Med-High

(详细 pros/cons 见 office-hours session 上下文)

## §6 · Recommended Approach (B · W1 + W3 详细设计)

### §6.1 · W1 数据层 · subtask 一等公民

**Frontmatter schema 演进**(`docs-cockpit-author` §2 更新):

```yaml
subtasks:
  # 旧格式 · 仍然支持 · validator 看到 string 当 title · 自动生成 id
  - "Lane A · BrowserVendor abstraction"

  # 新格式 · 推荐
  - id: M09-S1
    title: "Lane A · BrowserVendor abstraction"
    status: done                  # not-started | in-progress | done | blocked
    code: sourcery/worker/browser_vendor.py:42-89
    docs:
      - docs/spec/module/M09-browser-cluster.md#§2.1
      - docs/RFC/RFC-004-browser-vendor-abstraction.md

  # code 也支持 list (一个 subtask 跨多文件)
  - id: M09-S2
    title: "Lane B · LocalPlaywrightVendor"
    status: done
    code:
      - sourcery/worker/local_playwright_vendor.py
      - sourcery/worker/tests/test_local_vendor.py
    docs: [docs/spec/module/M09-browser-cluster.md#§2.2]
```

**`build.py::_build_card()` 改动**:
- 新增 `_normalize_subtasks(raw, module_id)`:把 string / dict 统一成 dict
  - string → `{id: auto-gen, title: string, status: not-started}`
  - **id 生成算法**:`<module-id>-<sha1(title)[:6]>` · **不用 body 顺序索引** · 否则用户在中间插入新 subtask 会让所有后续 id shift · 破坏 localStorage 持久化(localStorage 用 subtask.id 为 key)。Hash 算法的副作用:用户改 title 会换 id · 这是可接受 trade-off(改 title = 改任务定义 = 状态本来就该重算)
  - dict → 直接用 · `id` 缺失时按 hash 算法生成 · `status` 缺失默认 `not-started`
- subtask 拼装到 payload 时 · 解析 `code` ref · 调用新函数 `_resolve_code_anchor()`:
  - **`code` ref 格式决策(原 §7 Q1)** · 用 `path:start-end`(POSIX 风格 · grep 友好 · 跟 ripgrep / VS Code Quick Open 一致)· build 时同时生成 GitHub URL fallback(`<github-base>/blob/<branch>/<path>#L<start>-L<end>`)嵌入 payload · GitHub base 从 `docs-cockpit.yaml::repo_url` 读
  - 校验 `path` 存在(repo root 相对)
  - **路径处理统一 severity**:文件不存在 → **warn**(`severity: warn` · 不阻塞 build)· 这覆盖两种情况:用户笔误 + 后期 rename/delete · drawer 渲染该 subtask 时显示 `⚠ stale anchor` badge · 但 subtask 本身仍渲染 · status / docs / prompt 仍可用
  - 文件存在但行号越界 → **warn** · 行号显示 + badge
  - 路径存在 + 行号合法 → 读对应行 ±5 行 · 嵌入 `code_preview` 字段(长度 hard cap 800 字符 · 超出截断 + `…` 标记)
  - **Defensive IO error handling(plan-eng-review 3A)**:任何下列 IO 异常一律 **warn + None preview**(build 不炸)· drawer 显示 `⚠ preview unavailable` badge:
    - `OSError` / `PermissionError`(文件被锁 / 权限不足)
    - `UnicodeDecodeError`(文件不是 utf-8 · 例如 GBK / latin-1)
    - 文件前 1024 字节含 null byte → 判定 binary · skip
    - 文件 > 5MB → skip(超大文件 build 卡顿)
    - 行号越界(start > 文件行数)→ skip
  - **Performance(plan-eng-review 4A)**:`paths.py::_read_code_lines(path, start, end)` 用 `@functools.lru_cache(maxsize=256)` 包装 · 多 subtask 引用同 `path:lines` 不重复读 · build wall-time 节省 1.5-3x

**`validate_meta()` 改动**:
- 新增 §2.4 subtask schema 校验:
  - `id` 唯一性(单 module 内)· error 级别(同 id 会破坏 localStorage 状态)
  - `status` enum · error 级别(不在枚举内是输入错误)
  - `code` 路径存在性 + 行号合法性 → **warn** 级别(见上)
  - `docs` 路径走现有 `_resolve_doc_path()` 三步 fallback · path 不存在 warn
- `Issue.reference` 指向 `docs-cockpit-author · §2.4`

**`## 3 · 待办` body fallback 增强**(`build.py::extract_subtasks_from_body()`):

新内联语法:

```markdown
## 3 · 待办

- [x] Lane A · BrowserVendor abstraction @code:sourcery/worker/browser_vendor.py:42-89 @docs:M09-S1
- [x] Lane B · LocalPlaywrightVendor @code:sourcery/worker/local_playwright_vendor.py
- [ ] Lane F · Account/Proxy vendor_refs @code:sourcery/worker/account.py
```

- `[x]` / `[ ]` → status (done / not-started)
- `@code:<path>:<lines>` → subtask.code(支持多个)
- `@docs:<ref>` → subtask.docs(支持多个)
- id 自动生成 · **跟 frontmatter normalize 统一用 `<module-id>-<sha1(title)[:6]>`** · 不再用 body 顺序索引 · 这样混源 module(部分 subtask 在 frontmatter 部分在 body)id 计算一致 · 不冲突不漂移

**HTML drawer 渲染**(`templates/index.html.tmpl`):

```
[Subtasks · 5/8 done]
☑ M09-S1 · Lane A · BrowserVendor abstraction
   📁 sourcery/worker/browser_vendor.py:42-89  [open in VS Code]
   📄 M09-spec §2.1 · RFC-004
☑ M09-S2 · Lane B · LocalPlaywrightVendor
   📁 sourcery/worker/local_playwright_vendor.py
☐ M09-S6 · Lane F · Account/Proxy vendor_refs    [Copy prompt]
   📁 sourcery/worker/account.py
```

- `status` select 改 localStorage 用 `subtask.id` 为 key(不再用 index · 跨 build 稳定)
- `code` 图标点击:
  - 用户在 VS Code 打开 cockpit · 触发 `vscode://file/...` 深链
  - 用户用浏览器打开 · fallback 复制 path 到剪贴板 + toast
- `docs` 图标:点开打开关联 doc drawer(已有机制 · 直接复用)
- 每个 in-progress subtask 旁边显示 `[Copy prompt]` 按钮(进 §6.2)

### §6.2 · W3 Prompt scaffolding

**新模块** `docs_cockpit/prompt.py`:
- 依赖 Jinja2 · 加到 `pyproject.toml` `[project] dependencies`
- **Sandbox 必选**:用 `jinja2.sandbox.SandboxedEnvironment`(不是 default `Environment`)· 防止用户 template 用 `{% include %}` 任意读 fs · docs-cockpit 是 MIT 工具会在下游 untrusted repo 上跑 build · 安全边界重要
- **模板搜索路径白名单**:Loader 只允许两个目录 — `docs/prompts/`(用户)和 `docs_cockpit/templates/prompts/`(内置)· 用 `ChoiceLoader([FileSystemLoader('docs/prompts'), PackageLoader('docs_cockpit', 'templates/prompts')])`
- 函数 `render_prompt(module, subtask, repo_root, template_path?) -> str`
- Context vars 注入:
  - `module`:module 全 frontmatter + body
  - `subtask`:当前 subtask dict + code_preview(W1 已读)
  - `linked_docs`:`subtask.docs` 解析后的 doc 对象 list · **每个 doc 的 body summary hard cap 2000 字符**(取 §1 + §2 + §3 头部 · 超出截断 + `…` 标记 + 在 prompt 末尾加「完整 doc 见 `<path>`」hint)· 因为 prompt embed 走 sidecar(见 §6.3)· 这个 cap 控制 sidecar 单 entry 体积
  - `repo_root`:绝对路径
  - `current_branch`:**lazy + try/except** · `try: subprocess.check_output(['git', 'rev-parse', '--abbrev-ref', 'HEAD'], stderr=DEVNULL).decode().strip() except: None` · CI shallow-clone / 从 tarball build / 非 git 场景 fallback 为 `None` · template 用 `{% if current_branch %}...{% endif %}` 守护

**Context vars stability contract(plan-eng-review 2A)**:这是开源工具 · 下游 Sourcery + bastion + 第三方用户会写自定义 template · docs-cockpit 升级时不能静默 break:

1. **No-remove / no-rename**:每个 minor release 不删除 / 不重命名现有 vars · 只能 deprecate(打 warning · 至少保留两个 minor 周期再删)· 大版本(major)可以删 · 需 CHANGELOG 显式列
2. **New vars 必须可选**:新 var 引入时 · 内置 template 必须用 `{{ new_var | default('') }}` 或 `{% if new_var %}...{% endif %}` 守护 · 这样老用户 override template 不引用新 var 也能跑
3. **SandboxedEnvironment 配 `Undefined` (不是 `StrictUndefined`)**:用户 template 引用未定义 var 不 raise · 而是渲染为空 string · 优先 graceful degrade
4. **Schema 写到 `docs-cockpit-author/SKILL.md` §10.2**:列出 v0.X 提供的所有 vars · 每个 var 标 since-version · 每次 minor release 同步更新

这条契约写到 `references/prompt_templates.md` · 用户可以验证 · 升级有把握。

**Template 寻找顺序**(`prompt.py::_resolve_template`):
1. subtask frontmatter `prompt:` 显式指定(`docs/prompts/<custom>.md.j2`)
2. module frontmatter `prompt:` 显式指定
3. module frontmatter `prompt_kind:` 枚举(`feature` / `fix` / `refactor`)→ 用 docs-cockpit package 内置 template
4. fallback:`tpl/generic.md.j2`(docs-cockpit 内置)

**内置 templates**(`docs_cockpit/templates/prompts/`):
- `generic.md.j2` · 通用
- `feature.md.j2` · 实现 feature 子任务
- `fix.md.j2` · 修 bug 子任务
- `refactor.md.j2` · 重构子任务

每个 template 结构(generic 示例):

```jinja
你正在 docs-cockpit 驱动的 sprint **{{ module.sprint }}** 中执行子任务 `{{ subtask.id }}`。

## 上下文 · Module
- **Module**: {{ module.id }} · {{ module.title }}
- **目录**: {{ module.code_dir or 'N/A' }}
- **Status**: {{ module.status }} · Progress: {{ module.progress }}%
- **Spec**:
{{ module.body_summary }}

## 上下文 · 子任务
- **Subtask**: {{ subtask.id }} · {{ subtask.title }}
- **Status**: {{ subtask.status }}
{% if subtask.code %}
- **代码定位**:
{% for ref in subtask.code %}
  - `{{ ref.path }}`{% if ref.lines %}:{{ ref.lines }}{% endif %}
{% if ref.preview %}
    ```
    {{ ref.preview }}
    ```
{% endif %}
{% endfor %}
{% endif %}

{% if linked_docs %}
## 上下文 · 关联文档
{% for doc in linked_docs %}
### {{ doc.id }} · {{ doc.title }} ({{ doc.type }})
{{ doc.summary }}
{% endfor %}
{% endif %}

## 任务
请完成 `{{ subtask.id }}`:**{{ subtask.title }}**。

完成后,请输出 frontmatter patch(只输出变更字段),格式:

```yaml
# patch for {{ module._source_path }}
subtasks:
  - id: {{ subtask.id }}
    status: done   # 改这里
```

用户会审阅后手动落回 MD。
```

**CLI 子命令** · `docs-cockpit prompt`:
```bash
docs-cockpit prompt M09 M09-S6              # 子任务 prompt 输出到 stdout
docs-cockpit prompt M09 M09-S6 --copy       # 同时复制到剪贴板(pyperclip)
docs-cockpit prompt M09                     # 整 module 级 prompt
docs-cockpit prompt --list                  # 列所有可用 template
```

**HTML drawer 「Copy prompt」按钮**:
- build 时为每个 subtask 渲染好 prompt · **不内嵌主 HTML · 走 sidecar(详见 §6.3)**
- 浏览器点击 →
  1. `fetch('prompts.json').then(d => d['<subtask.id>'])` 拿渲染好的字符串
  2. 复制策略两层 fallback:
     ```js
     async function copy(text) {
       try { await navigator.clipboard.writeText(text); return true; }
       catch (e) {
         const ta = document.createElement('textarea');
         ta.value = text; ta.style.position='fixed'; ta.style.opacity='0';
         document.body.appendChild(ta); ta.select();
         const ok = document.execCommand('copy');
         document.body.removeChild(ta); return ok;
       }
     }
     ```
- **`file://` 兼容性**:`navigator.clipboard` 在 `file://` origin 大部分浏览器禁用 · cockpit 默认 file 协议打开 · 所以 `execCommand('copy')` fallback 是 primary path · 即使 deprecated 也必须保留
- toast 提示 success / failure(失败时 toast 显示 prompt 字符串前 200 字符 + 「请手动复制」)

**Lint 扩展** · `docs-cockpit lint --prompts`:
- 校验所有 `prompt:` 字段引用的 template 文件存在
- 校验 Jinja2 syntax · 校验引用的 context vars 在 schema 内

### §6.3 · HTML 嵌入策略 · sidecar + script-tag

**问题**(reviewer 指出):rendered prompt + code_preview + linked_docs summary 全部 inline 到 `state.json` payload · Sourcery 24 modules × 8 subtasks × ~(2KB prompt + 1KB preview + 2×2KB docs) ≈ 2-2.5MB inline JSON · `__DOCS_JSON__` 单 token 替换会膨胀 HTML 到 3MB · 跟 §8 「+30% 体积」目标冲突 · 浏览器解析慢 · grep diff 难看。

并且:任何 user MD / template / code preview 含字面串 `__DOCS_JSON__`(本 dogfood 项目里讨论 docs-cockpit 自己时极容易出现)都会破坏单 token 替换。

**决策**:

1. **主 payload 嵌入用 `<script type="application/json" id="docs-data">…</script>` · 不再用 `__DOCS_JSON__` 字符串替换**。
   - `templates/index.html.tmpl` 把 `__DOCS_JSON__` 占位符放到 `<script type="application/json" id="docs-data">__DOCS_JSON__</script>` 块内 · JSON 体内任何字面串(包括 `__DOCS_JSON__` 出现)都不会触发二次替换 · 因为 `template.replace()` 在 Python 端只跑一次
   - JS 读取:`const data = JSON.parse(document.getElementById('docs-data').textContent)`
   - 这同时解决了 collision · build.py 不需要换 sentinel(`__DOCS_JSON__` 仍然是 sentinel · 但只在 template 那一个位置存在 · `_render_html()` 用 `replace()` 仍然只 replace 一次)
   - 兜底:build 时 `escape` payload 里的 `</script>` 串(把 `</` 换成 `<\/`)· 防止 JSON 内出现 `</script>` 提前关闭 script tag

2. **prompt rendered string 拆到 sidecar `docs/prompts.json`**:
   - build 时单独输出 `docs/prompts.json`:`{"<subtask-id>": "<rendered prompt string>", ...}`
   - 主 HTML 不内嵌 prompt body · 只内嵌 subtask metadata(id / title / status / code / docs / has_prompt)
   - drawer 「Copy prompt」点击 → `fetch('prompts.json')` · 浏览器缓存友好 · 主 HTML 体积稳定
   - **file:// 限制**:`fetch('prompts.json')` 在 `file://` 部分浏览器(Chrome strict 模式)阻塞 · 兜底:build 时同时输出 `docs/prompts.js`(`window.__PROMPTS__ = {...}`)· HTML 用 `<script src="prompts.js" defer></script>` 引入 · 然后 drawer 直接读 `window.__PROMPTS__[id]` · 不走 fetch
   - 这个 JS sidecar 也走 sandbox safe content(只是 string map · 无 eval)

3. **code_preview** 走同一 sidecar `docs/code_previews.json`(或合并到 `prompts.js` 里 `window.__CODE__`)· 同上 file:// fallback

**体积预算**(决策后):
- 主 HTML(template + module/concept metadata + sprint/system_docs)· Sourcery 估算 ~600KB · 跟 0.10 基本持平
- `prompts.js`(24 × 8 × ~2KB)· ~400KB
- `code_previews.js`(24 × 8 × ~0.8KB)· ~160KB
- 总产物体积 ~1.2MB · 但主 HTML 单文件可独立打开 · drawer 「Copy prompt」点击才触发加载 prompts.js(via dynamic `<script>` inject 兜底 file:// fetch 限制)

**Trade-off**:用户不能再「打开 index.html · 完全断网工作」· 必须保留同目录的 `prompts.js` / `code_previews.js`。这是可接受的(本来就是 build 产物一起 ship)· 但要在 CHANGELOG + README 主推 banner 明确说明 · 0.10 的单文件叙事是 lead 卖点 · 不能闷头改。

**Graceful degrade(must)**:`prompts.js` 缺失时主 HTML 不能炸 · drawer 「Copy prompt」按钮 grey out + tooltip 「prompts.js 未加载 · 请确认 build 产物完整」· dashboard 其余功能(metadata / status / docs drawer)正常工作。

**Race condition handling**:`prompts.js` 用 `<script src="prompts.js" defer>` 在 `</body>` 前引入 · drawer 「Copy prompt」按钮 click handler 第一行 `if (!window.__PROMPTS__) { toast('prompts.js still loading, retry in a moment'); return; }` · 不假设加载顺序 · 不阻塞 dashboard 初渲染。

**Module 默认 lazy load**:如果用户 cockpit 实例很大(>50 modules)· `prompts.js` 也可能 >1MB · 留 v0.12 实现 per-module sidecar (`prompts/<module-id>.json` shard)· v0.11 单 sidecar 够用。

### §6.4 · 数据迁移 · 向后兼容

**Migrate path**:
- 已有 `subtasks: list[str]` 的 MD · build 时自动 normalize · 旧文件不需要改 · 但 cockpit 显示「⚠ 这个 subtask 缺 id/code/docs · run `docs-cockpit migrate-subtasks <file>` 升级」
- 新增 CLI `docs-cockpit migrate-subtasks <file>`:
  - 读 frontmatter + `## 3 · 待办` body
  - 对每个 string subtask · 用 LLM-free 启发(grep module 目录下 file/symbol)生成候选 `code:` ref
  - dry-run 默认 · 输出 diff · 用户 review 后 `--apply` 落盘
- 不强制迁移 · 老用户继续 v0.10 体验工作

### §6.5 · 文档与 skill 更新

- `docs-cockpit-author/SKILL.md`:
  - §2.4 加 subtask 对象 schema
  - §10 新增 prompt template 章节(template 寻找顺序 / context vars / 内置 templates)
- `docs-cockpit/SKILL.md`(主 skill):加 prompt scaffolding 触发条件 + CLI 用法
- `references/frontmatter_conventions.md`:加 subtask schema 速查
- `references/prompt_templates.md`:新文件 · 列内置 templates + 写自定义 template 指南
- `CHANGELOG.md` 0.11.0 section · 强调 schema 向后兼容 + W2 留给 v0.12
- `README.md` / `README.zh-CN.md`:加「v0.11 AI 协作驾驶舱」section + 截图

### §6.6 · UI 重构 · split-view 二级页面(2026-05-18 round-3 fold-in)

**问题**:0.10 的 module drawer 是 single-column modal · 最大 960px ≈ 50% 视口。subtask checklist + code anchor + linked docs + (v0.11) Copy prompt 按钮全挤在一个细长条里 · 信息密度极低。系统文档抽屉里关联文档是 `<a target="_blank">` · file:// 协议下浏览器只 dump raw markdown · 不渲染。**没有真正的「驾驶舱」体验** · 用户从 dashboard 跳出去看 spec / plan 上下文断裂。

**决策**:点击 module / subtask / systemDocs 任意 row · 不弹 modal · 而是切到二级页面 `<page-route>` · split layout:

```
┌─ Topbar(常驻) ────────────────────────────────────────────────────────┐
│ ← 返回 dashboard         Project Dashboard         System Docs  EN  + │
├──────────────────────┬─────────────────────────────────────────────────┤
│ Left · Navigator     │ Right · Preview                                  │
│ (从原 drawer 内容来) │ (自动渲染当前 active 文档)                       │
│                      │                                                  │
│ # M01 · Build Engine │ # P-v0.11-driver-seat.md                         │
│                      │                                                  │
│ Status: in-progress  │ ## §1 · Problem Statement                        │
│ Sprint: 0.11         │ docs-cockpit 现状是一个能渲染 module/concept...  │
│ Progress: 85%        │                                                  │
│                      │ ## §2 · What Makes This Cool                     │
│ ## Subtasks · 3/10   │ ...                                              │
│ [ ] v0.10 build...   │                                                  │
│ [✓] frontmatter...   │ (marked.js 渲染 · 跟 0.7.1 drawer 内联预览同款) │
│ ...                  │                                                  │
│                      │                                                  │
│ ## 关联文档 · 4 项   │                                                  │
│ ► P-v0.11-driver-... │  ← 当前 highlighted · 右栏显示这个               │
│   P-v0.11-test-plan  │  ← 点击切换右栏                                  │
│   Frontmatter conv...│                                                  │
│   Design tokens      │                                                  │
└──────────────────────┴─────────────────────────────────────────────────┘
```

**实现路径**(在单文件 HTML 内 · 不引入 router 框架):

1. **路由**:URL hash 作为软路由 · `#/module/M01` / `#/sysdoc/claude-md` / `#/concept/C01` · 浏览器后退键 work · 复制 URL 分享 work。 `window.location.hash` 监听 + 解析。
2. **Layout**:topbar 常驻 · main 区域用 `display: grid; grid-template-columns: minmax(360px, 38%) 1fr` · 左 navigator 380-450px 宽 · 右 preview flex 占满。
3. **左 Navigator**:复用现 `.drawer-body` 内容(desc / status select / progress slider / subtasks checklist / linked docs list) · 但 max-width 不再限 · 自适应。
4. **右 Preview**:复用现 `.doc-preview-body` (line 893+) CSS · 内容来自 active doc 的 `content` 字段 · marked.js 渲染。**默认显示第一条关联文档** · 用户点其他 row 切换。
5. **响应式**:< 1024px viewport 退化为 stacked 单列(左在上 / 右在下 · 或者左变成 selector dropdown 右常驻) · 移动端不丢功能。
6. **从 dashboard 进入**:module card / system docs row 的 click handler 改 `location.hash = '#/module/M01'` 而不是 `drawer.open()`。
7. **从二级页面返回**:左上角「← 返回 dashboard」按钮 + Esc 键 · `location.hash = ''`。
8. **向后兼容**:原 modal drawer 留作 fallback(`?ui=modal` URL query 切回 · 或者 < 768px 极窄屏)· 避免用户 muscle memory 完全打破。

**与 W1 / W3 的集成**:
- subtask 渲染(W1 §6.1 写的 status select + code anchor + docs ref icon)放在左 navigator · 不挤
- 「Copy prompt」按钮(W3 §6.2)放在每条 subtask 末尾 · 右栏自动显示 rendered prompt preview(可选)
- code anchor 点击除了 `vscode://file/...` 深链 · 也支持在右栏 inline 预览 code preview snippet

**Trade-off**:
- 单文件 HTML 仍保持 · 不引入 SPA 框架 · 不破坏「双击 file:// 即可看」叙事
- 主 `index.html` 体积预计 +50KB(新增 split layout CSS + hash router JS + 双栏渲染逻辑)· 仍在 §8 +20% budget 内
- 移动端体验 graceful degrade · 不优化 · 这是桌面侧驾驶舱工具
- 路由用 hash 不用 history API · file:// 协议下 history.pushState 受限

**实现工程量**:1-1.5 周(主要在 template 改 + JS hash router + 响应式 CSS)。

### §6.7 · systemDocs inline preview + payload 嵌入(2026-05-18 round-3 fold-in)

**问题**:`build_payload` 给 module 的 `docs:` 列表跑了 `_resolve_and_embed_docs` 把每条关联文档的 `content` 嵌入 payload。**system_docs 没经过这函数** · payload 里 system_docs 只有 `{id, title, path, desc, icon}` · 没 content。所以系统文档 drawer 渲染 `<a target="_blank" href="file:///CLAUDE.md">` · 浏览器新窗口只 dump 纯文本。

**决策**:

1. **build.py** · `build_payload` 给 system_docs 也跑同款 `_resolve_and_embed_docs` 逻辑 · 输出 `systemDocs[].content` + `systemDocs[].mtime` + `systemDocs[].exists`
2. **HTML template** · `renderSystemDocs()` 改 click handler · `location.hash = '#/sysdoc/{id}'` · 进 §6.6 split-view 二级页面 · 右栏自动渲染 content · 跟 module 体验一致
3. **payload 体积**:CLAUDE.md ~10KB · README ~5KB · 7 个 system_docs 估算 +30-50KB · 在 §8 体积 budget 内 · 不需要 sidecar
4. **大文件保护**:单 doc content > 50KB 时截断 + 末尾加「完整 doc 见 `<path>`」hint(避免 ~/.claude/projects/memory/ 这种巨大 dir 把 payload 撑爆)

实现工程量:2-3 天(payload 改少 · 主要是 §6.6 split-view 完成后自然衔接)。

## §7 · Open Questions (deferred · not blocking implementation)

载荷格式 / 沙箱 / 嵌入策略 / id 算法已在 §6 决策。剩下的是真正可延后的问题:

1. **status 写回 MD** · localStorage 跟 MD 长期漂移
   - 留给 **v0.11.1 patch** · 新增 `docs-cockpit sync-status` CLI · 读 localStorage(用户从 cockpit 「Export state」按钮 download 出来的 JSON)· 跟 MD merge · 输出 diff · 用户 review 后 apply
2. **frontmatter patch 落回 MD** · v1 用户手动复制 · 体验 friction
   - 留给 **v0.12** · 新增 `docs-cockpit apply-patch` CLI 解析 Claude 输出的 patch yaml
3. **编辑器深链兼容性** · VS Code 用 `vscode://file/<abspath>:<line>` · Cursor 同 schema · 但 JetBrains / Codespaces / Sublime 不同
   - drawer 同时加 「Copy path」兜底按钮 · 不假设特定编辑器 · 决策已落 §6.1 · 这里只是 followup:**v0.12** 提供 `editor:` config 字段让用户切换 schema(`docs-cockpit.yaml` 加 `editor: vscode | cursor | jetbrains`)
4. **`prompts.js` 体积超 1MB 的 module 拆分** · 大型 cockpit (>50 modules) 主 sidecar 会膨胀
   - 留给 **v0.12** · per-module sidecar (`prompts/<module-id>.json` shard)· v0.11 单 sidecar 够 Sourcery (24) + bastion (49) 用

## §8 · Success Criteria

**用户侧**:
- [ ] Sourcery 项目升级到 v0.11 · 24 个 module 的 subtasks 全部带 `code:` ref · cockpit drawer 能直链到代码
- [ ] 用户在 cockpit 复制 prompt · 粘到 Claude · Claude 能凭 prompt 直接动手(不需要二次问"在哪里改 / 看哪个 spec")· 验收 3 个真实 subtask
- [ ] bastion 项目升级到 v0.11 · 49 个 module 同上验证
- [ ] 用户写新 subtask 时 · 用 `## 3 · 待办` 内联语法 `@code @docs` 一行搞定 · 无需 frontmatter

**工程侧**:
- [ ] 0.11.0 minor release · CHANGELOG / version 4 文件同步
- [ ] 向后兼容:Sourcery 当前 master(v0.10 格式)build 不报 error · 只有「建议升级 subtask schema」warn
- [ ] `docs-cockpit lint --prompts` 通过 · 所有内置 template 渲染 ok
- [ ] **主 `index.html` 体积** 增加不超过 +20%(只 inline metadata · sidecar 走独立 file · 见 §6.3)
- [ ] **`prompts.js` 体积** 在 Sourcery (24 mods × ~8 subtasks)上 ≤ 600KB · 在 bastion (49 mods)上 ≤ 1.2MB · 超过触发 v0.12 per-module shard 计划
- [ ] **build wall-time** 在 Sourcery 上不超过 v0.10 build 的 1.5x(prompt 渲染 + code preview 读 fs 是新增成本)
- [ ] **安全**:用户在 untrusted repo 跑 `docs-cockpit build` · 自定义 prompt template 不能用 `{% include '/etc/passwd' %}` 等手法读 sandbox 外文件(§6.2 `SandboxedEnvironment` + Loader 白名单覆盖)
- [ ] **Graceful degrade**:删除 `prompts.js` 后再 reload index.html · dashboard 主功能(metadata / drawer / status / docs)仍正常 · 只有 「Copy prompt」按钮 grey out · 不报错不白屏

**UI 侧(2026-05-18 round-3 加)**:
- [ ] **Split-view 二级页面**:点 module / subtask / sysdoc / concept 进入 `#/{kind}/{id}` 路由 · 左 navigator + 右 preview 并存 · 浏览器后退键回 dashboard work
- [ ] **systemDocs inline 预览**:点 CLAUDE.md / README / 任何 system doc · 右栏直接 marked.js 渲染 MD · 不再新窗口 dump raw text
- [ ] **关联文档自动渲染**:进入 module 二级页面默认显示第一条关联文档 · 点其他切换 · 不点关联文档列表里的 [预览] 按钮(那个按钮废弃)
- [ ] **响应式**:1024px 以下退化 stacked 单列 · 移动端 readable
- [ ] **向后兼容**:`?ui=modal` URL query 切回原 modal · 让重度用户 muscle memory 不破
- [ ] **Code anchor 预览**:W1 subtask 的 `code:` ref 在右栏渲染 code preview snippet + `vscode://file/...` 深链按钮

**叙事侧**:
- [ ] README v0.11 section + 截图 + 30 秒 demo gif · 在 GitHub 主页能讲清楚「驾驶舱」叙事
- [ ] 1 篇 blog 或 changelog 长文 · 解释「为什么 W2 不在 v0.11」(关于 trust 边界的产品 essay)

## §9 · Distribution Plan

- **Python CLI**:PyPI · `pip install --upgrade docs-cockpit==0.11.0`
- **Claude Code plugin**:`.claude-plugin/marketplace.json` + `plugin.json` bump · 用户走 `docs-cockpit upgrade` CLI(已有 0.7.0+ 原子升级路径)
- **CI**:无新增 · 现有 release workflow(如果有)覆盖
- **公告**:
  - CHANGELOG 顶部 + "Why" 段落(参考 v0.10 风格)
  - README 头部加 v0.11 banner
  - 给 Sourcery + bastion 用户(单点通知)· 附 migrate 指南

## §10 · Dependencies

- **Jinja2** · 新增 runtime 依赖 · pyproject.toml `[project] dependencies` 加
- **pyperclip** · `--copy` flag 用 · optional dep · 未安装时 CLI 输出 prompt 到 stdout + stderr 提示 `--copy disabled · pip install pyperclip to enable`(不 raise)
- **pytest / pytest-cov / playwright**(plan-eng-review 4A)· dev dep · `pyproject.toml [project.optional-dependencies] dev = ["pytest>=7", "pytest-cov", "playwright", "pyperclip"]`
- **GitHub Actions**(plan-eng-review 4A)· `.github/workflows/test.yml` matrix 3 Python × 3 OS · 走 `pip install -e .[dev] && pytest`
- **(必需 prerequisite · 见 §11 Step 0)** · 本 plan 落地前 docs-cockpit 自己必须先 dogfood 一次(创建 docs/ + module/ + concept/)· 否则本 design doc 自己孤立 · 形不成可视化驾驶舱

## §11 · Next Steps

**Step 0 · Prerequisite · Dogfood docs-cockpit 自己(0.5 周 · 不计入 v0.11 3 周 budget)**

这是 prerequisite · 不是 v0.11 的 deliverable · 单独 ship 在一个 patch release(例如 0.10.1)· 否则本 design doc 在 docs-cockpit 自己的 repo 里是孤立 markdown · 形不成可视化驾驶舱:

- 用 `docs-cockpit init` 在 repo 根创建 `docs-cockpit.yaml`
- 用 `docs-cockpit migrate` 把现有 CLAUDE.md / README / references/ 当 system_docs
- 建 `docs/spec/module/` · 把 docs-cockpit 自己拆成 ~6 个 module(build_engine / cli / plugin / author_skill / portfolio / browse_reader)
- 让 docs-cockpit 自己的 cockpit 在 `docs/index.html` 可用
- **本 design doc(`docs/plans/P-v0.11-driver-seat.md`)在 dogfood cockpit drawer 里渲染出来** · 这是 Step 0 的验收条件

Step 0 实际 ship 的是 **0.10.1 patch · 只含 dogfood bootstrap**(`docs-cockpit.yaml` + system_docs 接线 + 6 个 docs-cockpit-self module 骨架 · 不动 schema · 不动 build.py 行为)· 这一步如果暴露 schema 缺口(必然会有 — docs-cockpit 自己拆模块时会发现现有 frontmatter 不够用)· 那些缺口 **进入 v0.11 W1 的设计输入**(不 backport 到 0.10.x · backport schema break 跟 SemVer 冲突)。换言之:Step 0 验证 v0.10 schema 能否承载自己 · 找出的不足直接喂 v0.11。

**v0.11 主 budget(~5.5 周 · 不含 Step 0 · 增加由 plan-eng-review 1A + 4A + 2026-05-18 round-3 UI fold-in 决策)** ↓

**Step 1 · 测试基础 + build.py refactor(4-5 天 · 来自 plan-eng-review 1A + 4A)**

Beck 的「make the change easy, then make the easy change」· W1 落地前先把 build.py 切薄 + 立 test 基础:

- **拆 `docs_cockpit/build.py`** →
  - `docs_cockpit/schema.py`(纯 schema 校验 · no fs · 含 `validate_meta` + 新增 `normalize_subtasks` / `validate_subtask_schema`)
  - `docs_cockpit/paths.py`(路径解析三步 fallback · `resolve_path` + `resolve_code_anchor` · 含 defensive IO error handling · 见 §6.1)
  - `docs_cockpit/cli.py`(argparse dispatcher · 把 `main()` 从 build.py 抽出)
  - `build.py` 减肥到 ~600 行 · 只保留 `cmd_build` + `_build_card` + `build_payload` + `render_html`
- **新增 pytest 测试基础**:`tests/` 目录 + `pyproject.toml::[project.optional-dependencies] dev = ["pytest", "pytest-cov", "playwright", "pyperclip"]`
- **GitHub Actions CI matrix**:`.github/workflows/test.yml` · `python: [3.10, 3.11, 3.12]` × `os: [ubuntu-latest, macos-latest, windows-latest]` = 9 cells
- **Pre-commit hook**(可选)· `ruff` + `mypy` 走 dev dep · 不在 v0.11 内强制
- 验收:Sourcery + bastion 已有 v0.10 schema 在新 codebase 上 build 不变(0 regression)+ schema.py / paths.py 单元测试 100% 覆盖
- 0.11.0-alpha.1 内部 release(只 refactor + test infra · 用户视觉无感知)

**Step 2 · UI 重构 · split-view 二级页面 + systemDocs 嵌入(1-1.5 周 · §6.6 + §6.7)**

UI 先于 W1 数据层 · 因为 W1 的 subtask 一等公民必须在 split-view 才有意义 · 修宽框 modal 容不下 code anchor + status select + Copy prompt 这一行密集元素:

- `templates/index.html.tmpl` 加 hash router(`#/module/<id>` / `#/sysdoc/<id>` / `#/concept/<id>`)
- 加 split layout CSS · `display: grid; grid-template-columns: minmax(360px, 38%) 1fr`
- 左 navigator 复用现 `.drawer-body` 内容 · 自适应宽度不再限 960px
- 右 preview 复用现 `.doc-preview-body` (line 893+) 渲染样式 · marked.js 加载关联文档 content
- 默认右栏渲染第一条关联文档 · 点其他 row 切换 active state
- module card / system docs row click handler 改 `location.hash = ...` · 不弹 modal
- topbar 加「← 返回 dashboard」按钮 + Esc 键 hotkey 退出
- 响应式 < 1024px 退化为 stacked 单列(左在上 / 右在下)
- `?ui=modal` URL query 保留原 modal 行为(向后兼容)
- `build.py::build_payload` 给 system_docs 跑 `_resolve_and_embed_docs` · 输出 `content` + `mtime` + `exists`
- 单 doc content hard cap 50KB(防止 memory dir 撑爆 payload)
- **测试**:`tests/e2e/test_split_view.py` Playwright · navigate 触发 hash route · assert 右栏 marked.js 渲染 · click 切换 · Esc 返回 + 单元测 systemDocs embed
- 0.11.0-alpha.2 内部 release(UI 验收 · 用户能立刻体验到新 driver-seat 框架)

**Step 3 · W1 数据层(1.5 周 · §6.1)**

subtask 一等公民 · 在 Step 2 split-view 框架内渲染:

- `schema.py::normalize_subtasks()` + `validate_subtask_schema()`
- `paths.py::resolve_code_anchor()`(含 §6.1 defensive IO + `@functools.lru_cache` 包装 `_read_code_lines`)
- `## 3 · 待办` body parser 支持 `@code @docs` 内联(`build.py::extract_subtasks_from_body()`)
- split-view 左 navigator 渲染 subtask 对象 + code 图标 + status select(localStorage 用 subtask.id 为 key)
- 点 code anchor → 右栏渲染 code preview snippet(复用 marked.js 的 code block)+ `vscode://file/...` 深链按钮
- `docs-cockpit migrate-subtasks <file>` CLI 子命令(走新 `cli.py`)
- `docs-cockpit-author/SKILL.md` §2.4 + `references/frontmatter_conventions.md`
- **测试**:`tests/unit/test_schema.py` + `tests/unit/test_paths.py` + `tests/integration/test_build_v011.py`(见 P-v0.11-test-plan.md §3)
- 0.11.0-alpha.3 内部 release(Sourcery 试用 · 24 module 全部走新 schema)

**Step 4 · W3 prompt scaffolding(1.5 周 · §6.2)**

prompt 渲染 · 接入 split-view 「Copy prompt」按钮:

- `docs_cockpit/prompt.py` + Jinja2 SandboxedEnvironment + 4 个内置 template + **context vars stability contract**(见 §6.2)
- `docs-cockpit prompt` CLI 子命令
- split-view 左 navigator 每条 subtask 末尾加「Copy prompt」按钮 + 可选右栏渲染 rendered prompt 预览
- `prompts.js` sidecar 输出(§6.3) + `</script>` escape(0.10.1 已 ship · 在新 sidecar 模式下仍 defense-in-depth)
- `docs-cockpit lint --prompts`
- `docs-cockpit-author/SKILL.md` §10 + `references/prompt_templates.md`
- **测试**:`tests/unit/test_prompt.py`(含 SandboxedEnvironment / git unavailable / 4 templates)+ `tests/integration/test_cli_v011.py` + `tests/e2e/test_split_view.py` 扩展 Copy prompt 流程
- 0.11.0-alpha.4 内部 release(Sourcery + bastion 试用 · 完整 W1 + W3 + UI 体验)

**Step 5 · 收口 + 公告(0.5 周)**
- CHANGELOG 0.11.0 section · 强调三件事(split-view + subtask 一等公民 + prompt scaffolding)
- README / README.zh-CN v0.11 banner + 截图 + demo gif(必有 split-view 截图 + 一条 subtask 点 Copy prompt 的录屏)
- 4 文件 version 同步 bump 到 0.11.0(去掉 alpha 后缀)
- `docs-cockpit upgrade` 路径回归
- 0.11.0 正式 release

**v0.12 候选**(本 plan 不做 · 留窗口):
- W2 LLM 文档优化器(`docs-cockpit suggest`)
- MCP server · 让 Claude 直接消费 cockpit prompt(替代 copy-paste)
- `docs-cockpit apply-patch` · 把 Claude 输出的 frontmatter patch 自动落回 MD
- `docs-cockpit sync-status` · localStorage state.json 合并到 MD

## §12 · Implementation Handoff

approve 后的下一个 action 是 `/plan-eng-review` · 把本 design doc 喂进去 · 让 eng 视角再过一遍架构 / 测试覆盖 / 边界。eng-review 会找出:

- §6.1 `_normalize_subtasks()` 的具体函数签名 + 哪些字段需要测试 fixture
- §6.3 sidecar JS 注入的 race condition(`prompts.js` 还没加载完用户就点 Copy)
- §6.4 数据迁移工具 `docs-cockpit migrate-subtasks` 的 dry-run / apply 边界
- 测试矩阵 · Python 3.10 / 3.11 / 3.12 · Windows / macOS / Linux fs case-sensitivity 影响 `code:` ref 解析

eng-review 通过后 · implementation 按 §11 顺序推 · 每个 Step 收尾在 Sourcery 上 alpha 验证。

---

## Appendix A · Session observations (from /office-hours)

(/office-hours skill 要求的 reflective section · 不参与 implementation · 列在附录避免污染主 plan)

- 痛点是「用户写不出合格的 source MD」· 不是「cockpit 不够漂亮」· 引用用户原话:「使用docs-cockpit的用户的文档编写规范都是不足以支持 模块-任务-子任务-对应的spec plan rfc」· 反馈来自 Sourcery + bastion 真实使用
- 5 个诉求清单中「复制提示词执行」放在第 5 · 但实际是「驾驶舱」叙事的拐点 · 前 4 个都是它的前置
- 用户对 process 的容忍度跟范围匹配:小事(上一轮 24-module audit)直接动手 · 大事(本轮 v0.11 设计)主动 `/office-hours`
- 用户对 coverage 的偏好 · 跟 docs-cockpit 「Boil the Lake」开源工具定位一致

---

**Status:** APPROVED · 2026-05-17 · ready for implementation per §11 Step 0 → Step 4

**Review trail**:
- /office-hours (2 rounds reviewer · 6/10 → 10 fixed → 8/10 → 5 nits fixed) · APPROVED
- /plan-eng-review (4 sections · 4 issues + 1 minor folded · all decisions in-doc · companion test plan at `docs/plans/P-v0.11-test-plan.md`) · APPROVED
- /docs-cockpit:docs-cockpit dogfood (2026-05-18 round-3 · 用户实测发现:系统文档 inline 预览缺失 + module drawer 太挤 + 想要 split-view 二级页面体验)· §6.6 / §6.7 / §11 Step 2 fold-in · APPROVED

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | not run (scope locked in /office-hours) |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | not run (2 rounds /office-hours reviewer instead) |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR | 4 issues + 1 minor · all folded into plan |
| Dogfood Round-3 | `/docs-cockpit:docs-cockpit` | UI feedback (实测) | 1 | CLEAR | split-view + sysdocs inline preview · §6.6/§6.7 fold-in · Step 2 加 |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | not run (HTML drawer scope only · not new visual system) |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | not run |

**ENG REVIEW DETAIL**:
- 1A · build.py 单文件膨胀 → 拆 `schema.py` + `paths.py` + `cli.py` · 加到 §11 Step 1
- 2A · prompt template forward-compat → context vars stability contract · 加到 §6.2
- 3A · code preview IO error handling → defensive spec(OSError / UnicodeDecodeError / binary / >5MB / 越界)· 加到 §6.1
- 4A · v0.11 测试 ambition → pytest + CI matrix · 加到 §11 Step 1 + §10 dev deps
- Minor (folded · 不 ask) · `_read_code_lines` 加 `@functools.lru_cache` · 加到 §6.1

**UNRESOLVED:** 0
**CRITICAL GAPS:** 0
**VERDICT:** ENG CLEARED — ready to implement per §11 Step 0 → Step 4

**Out-of-scope reviews surfaced** during eng review(not run · noted for future):
- DX Review · if 0.11.0 ships then user-facing CLI 在 `docs-cockpit prompt --copy / --list / migrate-subtasks` 这一面值得跑 `/plan-devex-review` 验证 CLI ergonomics
- Design Review · 如果 drawer UI 改动比预期大(新增 status select / code anchor icon / Copy prompt button)· 落地阶段可考虑 `/design-review` 在 dogfood cockpit 上跑一次
